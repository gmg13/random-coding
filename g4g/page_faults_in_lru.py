# code
# howdy!! yeowhhww


class Node:
    def __init__(self, val):
        self.val = val
        self.next = None
        self.prev = None


sentinel = Node(None)


def pfcounts(arr, m):
    # define the root, set and index ll
    root = sentinel
    mem = set()
    index = 



def main():
    # get the number of cases
    ncases = int(input())
    # and loop through
    for _ in range(ncases):
        arrsize = int(input())
        # get the array elements
        arr = [int(i) for i in input().split()][:n]
        # get the memory size
        memsize = int(input())
        # and run the algorithm
        print(pfcounts(arr, memsize))


if __name__ == '__main__':
    main()
